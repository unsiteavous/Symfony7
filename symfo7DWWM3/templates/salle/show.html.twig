{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.nom }}</td>
            </tr>
            <tr>
                <th>Places</th>
                <td>{{ salle.places }}</td>
            </tr>
            <tr>
                <th>Accessibilite</th>
                <td>{{ salle.accessibilite ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'nom': salle.nom}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
