<header class="d-flex justify-content-between m-3 align-items-center">
	<div class="fs-1">
		LOGO.
	</div>
	<div> 
		{% if app.request.pathInfo != path('app_film_index') %}
			<a href=" {{ path('app_film_index') }}">Revenir à la liste des films</a>
		{% else %}
			
		{% endif %}
	</div>
</header>
